# 奎燕设计智能体 · 产品壳（工作室形态）

> 产品名：**KEY 视界**。智能体名：奎燕设计智能体。  
> 最终形态：可安装的 Windows / macOS 客户端（`desktop/`），同一套 `ui-shell` 也可当网页打开。

底层五步不变：问清 Brief → 检索自有库 → 分类打标 → 老板筛选 → 差异化报告。  
界面不再做成五步向导，避免把思维卡死。

## 布局（ChatGPT / Cursor 心智）

```
┌──────────┬────────────────────────────┬───────────────────┐
│ 任务栏    │ 人机对话                     │ 可收起的结果画布     │
│ KEY logo │ Brief 追问 / 重筛 / 拍板      │ 自有库 · 老板选 · 报告 │
│ 任务列表  │                             │                     │
│ 席位/权限 │                             │                     │
└──────────┴────────────────────────────┴───────────────────┘
```

- 左栏只放任务和席位，不放三个智能体看板。
- 中间是配置好的 LLM 对话。Brief 太粗就追问：人群、价格带、品牌定位、场景、线上/线下、0-1 还是升级。
- 右边是画布，不是第三套向导。默认打开自有库，方便看 452 张墙。

## 数据立场

- **只检索自有库**。不对外网站点（Behance / 花瓣 / 淘宝等）现场爬取。
- 库切片：国内在售、概念/飞机稿。头部体量、新锐增速本轮未标，空态写诚实缺口。
- 市场风格（东方 / 国际 / 简约 / 传统 / 时尚 / 专业…）映射已有 style-buckets，不重写 jsonl。
- 主墙 452 / 待复核 2680 本轮不改。跨界 0 不编造。

## 老板决策

短名单带评分（只按已有字段）。可拿掉、写批注、再筛一轮。报告可复制、可下载 txt。到报告为止，不出现包装完稿。

详见 `AGENT-LAYERS.md`。桌面端打包见 `desktop/README.md`。
