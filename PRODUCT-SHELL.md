# 奎燕设计智能体 · 产品壳（按最佳实践定稿 v1）

> 产品名：**KEY 视界**（设计竞品调研 · 方向遴选）。智能体名：奎燕设计智能体。全品类，非单品类工具。

参考用户「左中右」想法，并对照常见 Agent UX（侧栏 Agent / 主对话 / 画布式结果、可覆盖、过程透明）。

## 推荐布局（不是死板三等分）

```
┌─ Topbar: 项目/Brief 标题 · 阶段 · 共享短名单 ──────────────┐
├──────────┬────────────────────────────┬───────────────────┤
│ 左 220px │ 中 主对话（默认焦点）         │ 右 可折叠结果画布   │
│ Agent轨  │ 人提需求 / 追问 / 拍板        │ 地图·策略卡·参考   │
│ + 流水线 │ 建议 chip · 决策控件嵌入气泡  │ 随对话刷新         │
└──────────┴────────────────────────────┴───────────────────┘
```

### 为什么这样定
1. **对话居中**：用户心智仍是「跟一个人聊」；多 Agent 是幕后编排，避免一上来选 Agent。
2. **左栏是能力与状态，不是聊天列表**：展示编排器 + Crawler + Dot Dot 的角色/忙碌态/最近动作，可点「查看该 Agent 日志」，默认不强制切换人格聊天。
3. **右栏是 Canvas / 结果层**：市场地图、策略卡、短名单像 Cursor Diff / ChatGPT Canvas——对话驱动刷新，侯总在右侧 keep/kill。
4. **决策沉在对话里**：keep/kill 也可在气泡内完成，右栏同步；符合「override 友好」。
5. **过程透明**：左栏或对话里露出「正在采集 / 正在打标 / 已出三张卡」，降低黑盒感。

### 阶段（Stepper 可收在顶栏）
Brief 理解 → 采集中 → 市场地图 → 策略遴选 → 短名单交付

### 与现有工程关系
- L1–L4 流水线 = 右栏数据引擎
- Data Crawler / Dot Dot = 左栏能力节点
- 奎燕设计智能体 = 中栏唯一对用户说话的编排器

### 非目标（v1）
- 不做「每个 Agent 一个独立聊天室」默认模式
- 不做一步出包装完稿的大按钮
