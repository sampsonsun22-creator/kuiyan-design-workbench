# 奎燕设计智能体 · 五层竞品分析

> 产品名：KEY 视界。智能体：奎燕设计智能体。  
> 原则：先帮侯总**看全、选对、找到可做的差异化**；AI 不代替出包装完稿。  
> 工程锁：主墙 452 / 待复核 2680 本轮不改。五层是产品心智，不是把 jsonl 重写成五份。

巧克力、青绿茶、果汁都是举例。引擎必须品类无关：变的是本体、检索词、类比边，不是换一套系统。

## 五层（固定）

```
L1 意图识别  →  明确分析对象
L2 搜索穷尽  →  同类 / 不同类 / 跨界 铺开样本
L3 分类拆解  →  AI 多维打标（视觉 · 体验 · 商业）
L4 决策筛选  →  从万千里收 10 款级短名单，侯总可改意见重筛
L5 结论报告  →  用样本结构 + 短名单，写差异化机会（非完稿）
```

旧口径 L1–L4 把「筛竞品」和「出方向」挤在同一层。现口径：**L4 只负责选参考，L5 才出结论与方向假设。**  
现有三张方向卡（青绿新中轴 / 静奢留白 / 开箱仪式）挂在 **L5**，标注「方向示意 · 非完稿」。

---

### L1 意图识别

充分读 Brief，先钉死**分析对象**，再允许搜索。

必须澄清：

| 问什么 | 落到字段 |
|---|---|
| 这是什么产品 / 品类 | `input.product` · `intent.domain_id` |
| 卖给谁、在哪卖、什么价 | `audience` · `channel` · `price_band` |
| 气质与红线 | `culture_tone` · `must_have` · `must_avoid` |
| 奎燕会什么（先验） | `kuiyan_case_refs` + 品类 `kuiyan_fit` |
| 搜索该往哪扩 | `analogy_plan`（不同类 / 跨界）+ `query_plan` |

没有分析对象，不准假装已经「穷尽」。

---

### L2 搜索穷尽

核心战场是**同类**，但视野必须三路同时开。货架是同类里的在售切片，不是第四个品类。

**同类产品（核心）**  
占市场主导的知名品牌，以及设计新锐、正在试新方向的品牌。设计站 + 奖项 + 在售 listing 都算。

**不同类产品（同行业打开）**  
例如做果汁，看同属饮料的茶饮、咖啡包装，用来看行业趋势，避免只在本品类内卷。对应 ontology 的邻近 `analogy_seeds`。

**跨界产品（意料之外）**  
其他行业里做成 IP 或独特风格的爆款包装，给造型语言用。对应更远的 `cross_analogy_rules`（香氛、高端水、国潮美妆等）。

本轮诚实口径（青绿茶礼盒 · 452 主墙）：

| 路 | 角色口径 | 现状 |
|---|---|---|
| 同类 | `wallRole=primary` | **446** |
| 不同类 | `keep_analogy` | **2**（薄） |
| 跨界 | 尚未单列打标 | **0**（不编造） |
| 货架（在售切片） | `source_type=shelf` | **4** listing 样 |

穷尽 ≠ 已经采完。缺的路在 L5 报告里写进「下一步」，禁止在 UI 里假装已连通。

---

### L3 分类拆解

包装只是例子。任意品类都用同一套**维度骨架**，由 AI 打标；缺值必须显示「未标注」，不准用模板散文填。

**视觉与风格**

- 色彩：主色 / 辅色，明亮还是沉稳
- 造型与材质：瓶/盒/袋、烫金、覆膜、异形
- 图形与排版：抽象/具象、字体与信息层级
- 风格桶：沿用 `L3/style-buckets-v1.json` 约 20 桶（多标签）

**交互与体验**

- 开箱：开启是否顺、有无结构记忆点
- 功能实用：携带、储存、复用、防滑、便携
- 用户反馈：电商/社交槽点与好评（如「好看但容易漏」）

**销售与成本**

- 货架表现力：陈列是否吸睛
- 成本与定位：质感是否匹配价格带

机器合同见 `L3/classify-dimensions-v1.json`（壳内副本 `ui-shell/data/`）。本轮主墙 452 的真实覆盖：

| 维度 | 已标 / 452 |
|---|---|
| 色彩 | 8 |
| 造型与材质 | 19 |
| 图形与排版 | 17 |
| 风格桶 | 20 |
| 货架表现力 | 4（只有 `source_type=shelf` 才算） |
| 开箱 / 功能 / 用户反馈 / 成本 | **未采集** |

「未标注」和「本轮未采集」是两回事，UI 要分开写：前者是没标，后者是这条通道根本没开。两者都不许用模板散文顶上。

---

### L4 决策筛选

结合三件事收短名单（8–12 款，不是 452 全甩给侯总）：

1. 用户 Brief（必须有 / 必须避开 / 渠道）
2. 奎燕设计能力与过往案例（`kuiyan_case_refs`、品类 fit）
3. L3 标签与样本结构（同类为主，不同类 / 货架各留位置：有才收，没有不编）

每款必须带**推荐理由**，且只准引用真采到的字段：

```
路线：同类 / 不同类 / 货架
贴 brief：贴合（brief_relevance_v1=keep_core）
风格桶：中式典雅/礼赠（或「未标注」）
来源：京东 · shelf
检索词：青绿茶礼盒包装 green tea gift box
```

不准写「色块与留白节奏可借鉴」这种没有数据支撑的工艺散文。缺什么，就在同一张卡上写「还没标：色彩 / 造型与材质；开箱、用户反馈、成本本轮未采」。

侯总可以：

- 拿掉某一款（拿掉的不会在重筛里回来）
- 写下批注（例如「不要金红，多留白，只要中式现代的礼赠开箱」）
- 让 AI **按批注重筛**

**重筛 = 在已落地 452 墙上重排，不发起新采集。** 本版实现：把批注切成词，只认三处证据——标题 / 检索词 / 已标风格桶名，加一张「设计说法 → 风格桶」词表（留白 → 国际简约 / 极简白盒）。认不出的词直接说认不出（「『金红』这条本轮判不了，色彩多数未标注」），不假装筛过。

三张创意方向卡**不属于**这一层。

---

### L5 寻找差异化机会 · 结论报告

短名单定了之后，才用可复核的结构写结论：

1. **分析对象** — 品类 / 渠道 / 客群 / 红线  
2. **样本结构** — 同类 / 不同类 / 跨界 / 货架各多少，缺哪路  
3. **分类覆盖** — 哪些维度已打标、哪些仍是未标注  
4. **入选参考** — L4 短名单 + 理由  
5. **差异化机会** — 相对货架与同类的空白（方向假设可挂现有 3 张卡）  
6. **风险与下一步** — 深采、评论、跨界、成本

报告是决策备忘，不是完稿。没有短名单也可以出「覆盖缺口报告」，但必须标明「尚未完成筛选」。

---

## 和 KEY 视界壳的对应

工作室壳（`ui-shell/`，桌面端 `desktop/`）不再把五步画成向导。左栏是任务和席位，中间是对话（Brief 粗就追问），右边是可拖宽、可弹出的自有库 / 老板选 / 报告。五层仍是引擎，不是用户必须点完的页签。

| 层 | 对话里怎么出现 | 画布页签 | 已落地的界面 |
|---|---|---|---|
| L1 | 追问人群 / 价格带 / 定位 / 场景 / 线上线下 / 0-1 还是升级 | 问清 | 分析对象 / 客群渠道价 / 气质红线 / 奎燕先验 / 三路计划 vs 已落地 |
| L2 | 只检索自有库，不对外网站点新爬 | 自有库 | 同类 / 不同类 / 跨界 / 货架；另有国内在售、概念/飞机稿；头部/新锐未标则空态 |
| L3 | 市场风格映射已有桶（东方 / 国际 / 简约…） | 自有库 + 检查器 | 视觉 / 体验 / 商业三组维度，缺值写未标注 |
| L4 | 老板看评分、拿掉、写一句再筛 | 老板选 | 12 款短名单 + 字段级理由 + 拿掉 + 批注重筛 |
| L5 | 可复制、可下载的差异化报告 | 报告 | 六段：对象 → 样本结构 → 分类覆盖 → 入选参考 → 差异化机会（挂 3 张方向假设）→ 风险与下一步 |

一条底线：**界面上每个数字都要能回到 452 张墙上数出来。** 数不出来的，就写缺口。

## 明确不做（本轮）

- 不重写 452/2680 jsonl  
- 不新开 Firecrawl/Apify 去「补穷尽」  
- 不编造跨界墙、用户评论、成本数字  
- 不把 L5 方向卡说成完稿  
