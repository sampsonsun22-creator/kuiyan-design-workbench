# 侯总「市场地图」信息架构（L3 视图）

原则：先帮侯总**看全、选对**，不是直接出完稿。任意 brief → 同一张地图。  
五层心智见 `AGENT-LAYERS.md`：本文件描述的是 **L2 样本在 L3 怎么铺成墙**。墙的三列（主品类/类比/货架）对应搜索上的同类 / 不同类 / 在售切片；跨界本轮未单列。

## 四墙 + 一短名单

```
┌─────────────────────────────────────────────┐
│ L1 意图摘要（品类 / 渠道 / 气质 / 价格带）      │
├─────────────┬─────────────┬─────────────────┤
│ 主品类墙      │ 类比墙        │ 货架墙            │
│ 按风格桶分列   │ 跨品类参考    │ 在售真品 CN       │
├─────────────┴─────────────┴─────────────────┤
│ AI 推荐桶（可解释：为何这 3–5 个桶）            │
├─────────────────────────────────────────────┤
│ → L4 方向短名单（策略卡入口）                  │
└─────────────────────────────────────────────┘
```

### 1) 主品类墙
- **数据**：`category_domain_id` = brief 主域；`source_type` ∈ inspiration/award/portfolio；`suggested_style_buckets`
- **布局**：列 = 风格桶（只显示命中≥1 的桶）；卡 = 封面图 + title + source
- **筛选**：桶、颜色粗维、结构标签、是否有图（`image_url` 非空）
- **空态**：该桶无样本 → 显示「可从类比墙借入」而不是空白死胡同

### 2) 类比墙
- **数据**：`analogy_from` 非空，或 query 来自 ontology `analogy_seeds` / `cross_analogy_rules`
- **布局**：按类比边 chip（感官近 / 礼赠场 / 健康无糖 / 中式现代 / 国际简约）分组
- **目的**：逼出跨品类造型语言，避免只在本品类内卷

### 3) 货架墙
- **数据**：`source_type=shelf` 且 `is_on_market=true`（或电商源）
- **布局**：信息锤 / 货架效率视角（主主视觉、主卖点是否一眼可见）
- **空态**：Phase A 可少；标明「待接入天猫/京东/抖音」

### 4) AI 推荐桶
- **输入**：L1 `style_prior` + 品类 `default_weight_hints` + 样本桶分布
- **输出**：3–5 个桶 id + 一句 why（中文）
- **交互**：侯总可钉选/排除桶，重排主墙

### 5) L4 短名单
- 从四墙多选参考（记录 item `id[]`）→ 生成策略卡（见 `L4/`）

## 卡片字段（墙内）
`id, image_url|thumbnail_url, title, author_or_brand, source, suggested_style_buckets(→中文), structure_tags?, color_roles?, analogy_from?`

## 与 L2 字段契约
见 `TAG-CONTRACT.md`。无图条目（如部分 zcool）可进列表，但不进封面墙主栅格。
